require("vars")     -- load variables first (monitors, apps, paths, themes)
require("settings") -- monitors · hl.config · curves · animations
require("rules")    -- workspace rules · window rules · layer rules
require("keybinds") -- all keybindings
require("execs")    -- env vars · autostart
